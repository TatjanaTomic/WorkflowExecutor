<UserControl x:Class="CreatorMVVMProject.View.Main.StepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CreatorMVVMProject.View.Main"
             xmlns:converters="clr-namespace:CreatorMVVMProject.Model.Class.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:StatusToColorConverter x:Key="statusToColorConverter"/>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.7*"></RowDefinition>
            <RowDefinition Height="0.3*"></RowDefinition>
            <RowDefinition Height="0.3*"></RowDefinition>
            <RowDefinition Height="0.3*"></RowDefinition>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding StepId}" 
                   Background="{Binding Status, Converter={StaticResource statusToColorConverter}, UpdateSourceTrigger=PropertyChanged}"></TextBlock>
        <TextBlock Grid.Row="1" Text="{Binding Status}" ></TextBlock>
        <TextBlock Grid.Row="2" Text="{Binding StepDescription}"></TextBlock>
        <Button Grid.Row="3" Content="Start Step" Command="{Binding StartStepCommand}" ></Button>
        <!-- <TextBlock Background="{Binding Status}" Grid.Row="0" Text="{Binding StepId}"></TextBlock> -->
        <!-- <TextBlock Grid.Row="1" Text="{Binding StepDescription}}"></TextBlock> -->
    </Grid>
</UserControl>
