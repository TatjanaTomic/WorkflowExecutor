<UserControl x:Class="CreatorMVVMProject.View.Main.StageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:step="clr-namespace:CreatorMVVMProject.View.Main" xmlns:main="clr-namespace:CreatorMVVMProject.ViewModel.Main" d:DataContext="{d:DesignInstance Type=main:StageViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#1d0536">
        <!-- <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions> -->

        <ListView ItemsSource="{Binding StepViewModels}" Background="#1d0536" VirtualizingPanel.ScrollUnit="Pixel" ScrollViewer.HorizontalScrollBarVisibility="Disabled" HorizontalContentAlignment="Stretch">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <step:StepView/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- <Button Grid.Row="1" Content="{Binding StageId}" HorizontalAlignment="Right"
                 Command="{Binding StartStageCommand}" Width="150" Height="50" Margin="20,20,20,20"/>
        -->
    </Grid>
</UserControl>
