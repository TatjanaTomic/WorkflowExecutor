<UserControl x:Class="CreatorMVVMProject.View.Main.StageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:step="clr-namespace:CreatorMVVMProject.View.Main" xmlns:main="clr-namespace:CreatorMVVMProject.ViewModel.Main" d:DataContext="{d:DesignInstance Type=main:StageViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>

        <ListView ItemsSource="{Binding StepViewModels}"
                  Background="{StaticResource DeepDarkColorBrush}"
                  VirtualizingPanel.ScrollUnit="Pixel"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  HorizontalContentAlignment="Stretch">

            <ListView.ItemTemplate>
                <DataTemplate>
                    <step:StepView/>
                </DataTemplate>
            </ListView.ItemTemplate>

        </ListView>
        
    </Grid>
</UserControl>
